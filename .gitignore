# Dependencies, build output, logs (any sub-project)
node_modules/
dist/
build/
*.log
.env
.env.local

# Bot — secrets & downloaded binary (never commit)
bot/config.json
bot/data/
bot/bin/yt-dlp

# Local launcher (not part of the project)
start.sh
