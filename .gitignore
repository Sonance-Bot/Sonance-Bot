# The website lives in its own repo (Sonance-Bot/sonance-website), checked out
# here as a sibling folder — not tracked by this repo.
website/

# Dependencies, build output, logs
node_modules/
dist/
build/
*.log
.env
.env.local

# Bot — secrets & downloaded binary (never commit)
bot/config.json
bot/data/
bot/bin/yt-dlp

# Local launcher (not part of the project)
start.sh
